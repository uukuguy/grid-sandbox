# Phase 2.5.2 简化认证系统实现计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 实现可选的简化认证系统，支持 API Key 认证模式，向后兼容无认证模式

**Architecture:**
- AuthConfig + AuthMode 配置驱动
- ApiKey 验证中间件
- 用户上下文注入
- 向后兼容：无认证模式仍可用

**Tech Stack:** bcrypt, sha2, tokio, axum

**参考:** openfang `crates/openfang-kernel/src/auth.rs`

---

## 任务 1: 创建认证模块结构

**Files:**
- Create: `crates/octo-engine/src/auth/mod.rs`
- Create: `crates/octo-engine/src/auth/config.rs`
- Create: `crates/octo-engine/src/auth/middleware.rs`
- Modify: `crates/octo-engine/src/lib.rs`

**Step 1: 创建认证模块入口**

```rust
// crates/octo-engine/src/auth/mod.rs

pub mod config;
pub mod middleware;

pub use config::*;
pub use middleware::*;
```

**Step 2: 运行 cargo check 验证模块创建**

Run: `cargo check -p octo-engine`
Expected: SUCCESS (new files detected)

**Step 3: 提交**

```bash
git add crates/octo-engine/src/auth/
git commit -m "feat(auth): create auth module structure"
```

---

## 任务 2: AuthConfig + AuthMode 定义

**Files:**
- Modify: `crates/octo-engine/src/auth/config.rs`
- Test: `crates/octo-engine/tests/auth_config_test.rs`

**Step 1: 编写配置和模式定义**

```rust
// crates/octo-engine/src/auth/config.rs

use chrono::{DateTime, Utc};
use sha2::{Sha256, Digest};
use std::collections::HashMap;

/// 认证模式
#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
pub enum AuthMode {
    /// 无认证（默认）
    #[default]
    None,
    /// API Key 模式
    ApiKey,
    /// 完整认证（保留给 octo-platform）
    Full,
}

/// 权限
#[derive(Debug, Clone, PartialEq, Eq)]
pub enum Permission {
    Read,
    Write,
    Admin,
}

impl Permission {
    pub fn from_str(s: &str) -> Option<Self> {
        match s.to_lowercase().as_str() {
            "read" => Some(Permission::Read),
            "write" => Some(Permission::Write),
            "admin" => Some(Permission::Admin),
            _ => None,
        }
    }
}

/// API Key
#[derive(Debug, Clone)]
pub struct ApiKey {
    pub key_hash: String,           // sha256 哈希存储
    pub user_id: Option<String>,    // 可选用户绑定
    pub permissions: Vec<Permission>,
    pub expires_at: Option<DateTime<Utc>>,
    pub created_at: DateTime<Utc>,
}

impl ApiKey {
    pub fn new(key: &str, user_id: Option<String>, permissions: Vec<Permission>) -> Self {
        let mut hasher = Sha256::new();
        hasher.update(key.as_bytes());
        let key_hash = format!("{:x}", hasher.finalize());

        Self {
            key_hash,
            user_id,
            permissions,
            expires_at: None,
            created_at: Utc::now(),
        }
    }

    pub fn with_expiry(mut self, expires_at: DateTime<Utc>) -> Self {
        self.expires_at = Some(expires_at);
        self
    }

    pub fn is_expired(&self) -> bool {
        if let Some(expires_at) = self.expires_at {
            expires_at < Utc::now()
        } else {
            false
        }
    }
}

/// 认证配置
#[derive(Debug, Clone, Default)]
pub struct AuthConfig {
    pub mode: AuthMode,
    pub api_keys: HashMap<String, ApiKey>,  // key_hash -> ApiKey
    pub require_user_id: bool,               // 是否要求用户隔离
}

impl AuthConfig {
    pub fn new() -> Self {
        Self::default()
    }

    pub fn with_mode(mut self, mode: AuthMode) -> Self {
        self.mode = mode;
        self
    }

    /// 添加 API Key
    pub fn add_api_key(&mut self, key: &str, user_id: Option<String>, permissions: Vec<Permission>) {
        let api_key = ApiKey::new(key, user_id, permissions);
        self.api_keys.insert(api_key.key_hash.clone(), api_key);
    }

    /// 验证 API Key
    pub fn validate_key(&self, key: &str) -> bool {
        if self.mode != AuthMode::ApiKey {
            return self.mode == AuthMode::None;
        }

        let mut hasher = Sha256::new();
        hasher.update(key.as_bytes());
        let key_hash = format!("{:x}", hasher.finalize());

        if let Some(api_key) = self.api_keys.get(&key_hash) {
            !api_key.is_expired()
        } else {
            false
        }
    }

    /// 获取用户 ID
    pub fn get_user_id(&self, key: &str) -> Option<String> {
        let mut hasher = Sha256::new();
        hasher.update(key.as_bytes());
        let key_hash = format!("{:x}", hasher.finalize());

        self.api_keys.get(&key_hash).and_then(|k| k.user_id.clone())
    }

    /// 获取权限
    pub fn get_permissions(&self, key: &str) -> Vec<Permission> {
        let mut hasher = Sha256::new();
        hasher.update(key.as_bytes());
        let key_hash = format!("{:x}", hasher.finalize());

        self.api_keys.get(&key_hash).map(|k| k.permissions.clone()).unwrap_or_default()
    }
}
```

**Step 2: 运行 cargo check 验证**

Run: `cargo check -p octo-engine`
Expected: SUCCESS

**Step 3: 编写基础测试**

```rust
// crates/octo-engine/tests/auth_config_test.rs

use octo_engine::auth::*;

#[test]
fn test_auth_mode_default() {
    let config = AuthConfig::default();
    assert_eq!(config.mode, AuthMode::None);
    assert!(config.api_keys.is_empty());
}

#[test]
fn test_api_key_validation() {
    let mut config = AuthConfig::new();
    config.mode = AuthMode::ApiKey;
    config.add_api_key("test-key-123", Some("user-001".to_string()), vec![Permission::Read, Permission::Write]);

    // 正确的 key 应该通过
    assert!(config.validate_key("test-key-123"));

    // 错误的 key 应该失败
    assert!(!config.validate_key("wrong-key"));

    // 无认证模式下，任何 key 都应该通过（向后兼容）
    let mut config_none = AuthConfig::new();
    config_none.mode = AuthMode::None;
    assert!(config_none.validate_key("any-key"));
}

#[test]
fn test_api_key_expiry() {
    use chrono::Utc;

    let mut config = AuthConfig::new();
    config.mode = AuthMode::ApiKey;
    config.add_api_key("expired-key", None, vec![]);

    // 修改为已过期
    if let Some(key) = config.api_keys.values_mut().next() {
        key.expires_at = Some(Utc::now() - chrono::Duration::hours(1));
    }

    assert!(!config.validate_key("expired-key"));
}

#[test]
fn test_get_user_id() {
    let mut config = AuthConfig::new();
    config.mode = AuthMode::ApiKey;
    config.add_api_key("key-001", Some("user-001".to_string()), vec![Permission::Read]);

    assert_eq!(config.get_user_id("key-001"), Some("user-001".to_string()));
    assert_eq!(config.get_user_id("key-002"), None);
}
```

**Step 4: 运行测试验证**

Run: `cargo test -p octo-engine auth_config_test`
Expected: PASS

**Step 5: 提交**

```bash
git add crates/octo-engine/src/auth/config.rs crates/octo-engine/tests/auth_config_test.rs
git commit -m "feat(auth): implement AuthConfig and AuthMode"
```

---

## 任务 3: 认证中间件实现

**Files:**
- Modify: `crates/octo-engine/src/auth/middleware.rs`
- Test: `crates/octo-engine/tests/auth_middleware_test.rs`

**Step 1: 实现认证中间件**

```rust
// crates/octo-engine/src/auth/middleware.rs

use crate::auth::{AuthConfig, AuthMode};
use axum::{
    body::Body,
    extract::Request,
    http::{header, StatusCode},
    middleware::Next,
    response::Response,
};

/// 用户上下文
#[derive(Debug, Clone)]
pub struct UserContext {
    pub user_id: Option<String>,
    pub permissions: Vec<crate::auth::Permission>,
}

/// 认证中间件
pub async fn auth_middleware(
    req: Request<Body>,
    next: Next,
    config: &AuthConfig,
) -> Result<Response, StatusCode> {
    match config.mode {
        AuthMode::None => {
            // 无认证模式，直接放行，注入匿名用户
            let mut req = req;
            req.extensions_mut().insert(UserContext {
                user_id: None,
                permissions: vec![],
            });
            Ok(next.run(req).await)
        }
        AuthMode::ApiKey => {
            // 验证 API Key
            let key = req
                .headers()
                .get("X-API-Key")
                .and_then(|v| v.to_str().ok());

            match key {
                Some(k) if config.validate_key(k) => {
                    let user_id = config.get_user_id(k);
                    let permissions = config.get_permissions(k);

                    let mut req = req;
                    req.extensions_mut().insert(UserContext {
                        user_id,
                        permissions,
                    });
                    Ok(next.run(req).await)
                }
                _ => Err(StatusCode::UNAUTHORIZED),
            }
        }
        AuthMode::Full => {
            // 完整认证（octo-platform 实现）
            Err(StatusCode::NOT_IMPLEMENTED)
        }
    }
}

/// 从请求中提取用户上下文
pub fn get_user_context<B>(req: &Request<B>) -> Option<UserContext> {
    req.extensions().get::<UserContext>().cloned()
}
```

**Step 2: 运行 cargo check**

Run: `cargo check -p octo-engine`
Expected: SUCCESS

**Step 3: 编写测试**

```rust
// crates/octo-engine/tests/auth_middleware_test.rs

use axum::{
    body::Body,
    extract::Request,
    http::{header, StatusCode},
    middleware::Next,
    response::Response,
};
use octo_engine::auth::*;

#[tokio::test]
async fn test_auth_middleware_none_mode() {
    let config = AuthConfig::new();
    let req = Request::builder().uri("/").body(Body::empty()).unwrap();
    let next = Next::new();

    let result = auth_middleware(req, next, &config).await;
    assert!(result.is_ok());
}

#[tokio::test]
async fn test_auth_middleware_api_key_success() {
    let mut config = AuthConfig::new();
    config.mode = AuthMode::ApiKey;
    config.add_api_key("valid-key", Some("user-001".to_string()), vec![Permission::Read]);

    let req = Request::builder()
        .uri("/")
        .header("X-API-Key", "valid-key")
        .body(Body::empty())
        .unwrap();
    let next = Next::new();

    let result = auth_middleware(req, next, &config).await;
    assert!(result.is_ok());
}

#[tokio::test]
async fn test_auth_middleware_api_key_missing() {
    let mut config = AuthConfig::new();
    config.mode = AuthMode::ApiKey;

    let req = Request::builder().uri("/").body(Body::empty()).unwrap();
    let next = Next::new();

    let result = auth_middleware(req, next, &config).await;
    assert!(result.is_err());
    assert_eq!(result.unwrap_err(), StatusCode::UNAUTHORIZED);
}

#[tokio::test]
async fn test_auth_middleware_api_key_invalid() {
    let mut config = AuthConfig::new();
    config.mode = AuthMode::ApiKey;
    config.add_api_key("valid-key", None, vec![]);

    let req = Request::builder()
        .uri("/")
        .header("X-API-Key", "invalid-key")
        .body(Body::empty())
        .unwrap();
    let next = Next::new();

    let result = auth_middleware(req, next, &config).await;
    assert!(result.is_err());
    assert_eq!(result.unwrap_err(), StatusCode::UNAUTHORIZED);
}
```

**Step 4: 运行测试**

Run: `cargo test -p octo-engine auth_middleware_test`
Expected: PASS

**Step 5: 提交**

```bash
git add crates/octo-engine/src/auth/middleware.rs crates/octo-engine/tests/auth_middleware_test.rs
git commit -m "feat(auth): implement auth middleware"
```

---

## 任务 4: 配置加载支持

**Files:**
- Modify: `crates/octo-engine/src/auth/config.rs`
- Modify: `crates/octo-server/src/config.rs`

**Step 1: 添加 serde 支持到 AuthConfig**

```rust
// 在 config.rs 中添加

use serde::{Deserialize, Serialize};

/// 认证模式 - 可序列化
#[derive(Debug, Clone, Copy, PartialEq, Eq, Default, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum AuthMode {
    #[default]
    None,
    ApiKey,
    Full,
}

/// 权限 - 可序列化
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum Permission {
    Read,
    Write,
    Admin,
}

/// API Key 配置（用于配置文件）
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ApiKeyConfig {
    pub key: String,                    // 原始 key（加载时哈希）
    pub user_id: Option<String>,
    pub permissions: Vec<String>,
    pub expires_at: Option<String>,
}

/// 认证配置 - 可序列化
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct AuthConfigYaml {
    pub mode: Option<AuthMode>,
    pub require_user_id: Option<bool>,
    pub api_keys: Option<Vec<ApiKeyConfig>>,
}

impl AuthConfigYaml {
    /// 转换为运行时配置
    pub fn to_auth_config(&self) -> AuthConfig {
        let mut config = AuthConfig::new();

        if let Some(mode) = self.mode {
            config.mode = mode;
        }

        if let Some(require_user_id) = self.require_user_id {
            config.require_user_id = require_user_id;
        }

        if let Some(api_keys) = &self.api_keys {
            for key_config in api_keys {
                let permissions: Vec<Permission> = key_config
                    .permissions
                    .iter()
                    .filter_map(|p| Permission::from_str(p))
                    .collect();

                config.add_api_key(&key_config.key, key_config.user_id.clone(), permissions);
            }
        }

        config
    }
}
```

**Step 2: 验证编译**

Run: `cargo check -p octo-engine`
Expected: SUCCESS

**Step 3: 提交**

```bash
git add crates/octo-engine/src/auth/config.rs
git commit -m "feat(auth): add serde support for config loading"
```

---

## 验收标准

1. ✅ AuthConfig + AuthMode 定义完成
2. ✅ API Key 验证功能正常
3. ✅ 认证中间件实现
4. ✅ 向后兼容：无认证模式仍可用
5. ✅ 所有测试通过

---

## 依赖

```toml
# crates/octo-engine/Cargo.toml

[dependencies]
sha2 = "0.10"
bcrypt = "0.16"      # 可选，用于密码哈希
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
axum = "0.8"
```

