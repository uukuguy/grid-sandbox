---
phase: 05.1-runtime-tier-adr
verified: 2026-05-02T00:00:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
---

# Phase 05.1: Runtime Tier ADR + Contract Test Parametrization — Verification Report

**Phase Goal:** 在 ADR-V2-017 三轨产品策略基础上增加契约执行强度差异化策略 — 起草 ADR-V2-025 (type: strategy), 评估 7 runtime 现状 + 划分主力档/样板档/参考档/冻结档, 同时把 `tests/contract/cases/test_chunk_type_contract.py` 从现 3 tests 扩展为 7-runtime 参数化, 与 tier review 同步用 (NEW-D2)。
**Verified:** 2026-05-02
**Status:** passed
**Re-verification:** No — initial verification

## VERIFICATION PASSED

All 4 ROADMAP success criteria verified against codebase artifacts. ADR-V2-025 Accepted, test parametrized over 7 active runtimes (hermes correctly excluded as frozen), CI matrix tier-gated, NEW-D2 closed in DEFERRED_LEDGER with all 4 commit hashes (T1 `7293868`, T2 `3ba59a1`, T3 `e6c993b`, T4 `c4440ef`). Both REQUIREMENTS.md REQ-IDs (CONTRACT-00, WATCH-05) marked complete with `- [x]`.

## Goal Achievement

### Observable Truths (must-haves)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | ADR-V2-025 Accepted; 4 tier × 7 runtime table + hermes frozen note | ✓ VERIFIED | `status: Accepted` line 5 of ADR; YAML frontmatter parses cleanly via `yaml.safe_load`; 4 distinct tier names (`主力档`, `样板档`, `参考档`, `冻结档`) all present; all 7 active runtime names (`grid`, `claude-code`, `nanobot`, `pydantic-ai`, `goose`, `claw-code`, `ccb`) present in Decision table; hermes appears 12 times incl. dedicated frozen-note row at line 82 + frozen-tier row at line 76. |
| 2 | §References cite ADR-V2-017 + ADR-V2-021 + ADR-V2-024 + NEW-D2 | ✓ VERIFIED | `grep -E 'ADR-V2-017\|ADR-V2-021\|ADR-V2-024\|NEW-D2'` → 27 hits. Explicit §References block at lines 184-189 cites all four; YAML `related:` field lists `ADR-V2-017`, `ADR-V2-021`, `ADR-V2-024`. ADR-V2-024 mentioned 7 times across body. |
| 3 | test_chunk_type_contract.py parametrized 7-runtime; CI tier-gated | ✓ VERIFIED | `pytest --collect-only` shows 9 items: 7 parametrized `test_chunk_type_contract[<runtime>]` cases (grid, claude-code, nanobot, pydantic-ai, goose, claw-code, ccb) + 2 guard tests (`test_whitelist_matches_adr`, `test_unspecified_is_zero`). CI workflow has 8 matrix `include` entries with `tier:` + `xfail:` fields (primary `xfail: 'false'` × 2, sample `xfail: 'true'` × 2, reference `xfail: 'true'` × 3, frozen `xfail: 'skip'` × 1). `continue-on-error: ${{ matrix.xfail == 'true' }}` lifts gate at job level. Hermes guarded via `if: matrix.runtime != 'hermes'` on every build/test step + early-return notice step. |
| 4 | NEW-D2 ✅ CLOSED in DEFERRED_LEDGER with commit hash; ≥7 parametrized cases | ✓ VERIFIED | DEFERRED_LEDGER.md line 450 marks NEW-D2 as `✅ CLOSED` referencing T1 `7293868`, T2 `3ba59a1`, T3 `e6c993b`. Status-change log line 377 has `2026-05-02 \| NEW-D2 \| 🟠 P1 → ✅ CLOSED`. Statistics summary line 394 records NEW- namespace closed=1. Commit hashes verified via `git log --oneline --all`. Pytest collection confirms 7 parametrized cases for `test_chunk_type_contract` (Plan T2 explicit acceptance: ≥7 for live test; ROADMAP "≥21" interpreted per Plan as upper bound when 3 functions × 7 runtimes — only the live test is parametrized as designed). |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `docs/design/EAASP/adrs/ADR-V2-025-l1-runtime-contract-tier-strategy.md` | New ADR (strategy, Accepted) with 4-tier × 7-runtime table | ✓ VERIFIED | 189 LOC, frontmatter valid YAML; status=Accepted; enforcement.trace points to test_chunk_type_contract.py + phase3-contract.yml; `affected_modules` lists all 7 runtime crates + hermes. |
| `tests/contract/cases/test_chunk_type_contract.py` | Module-level `@pytest.mark.parametrize` over 7 runtime names | ✓ VERIFIED | `ADR_V2_025_ACTIVE_RUNTIMES` constant (line 50-58, 7 runtimes in ADR-table order); `@pytest.mark.parametrize("runtime_name", ADR_V2_025_ACTIVE_RUNTIMES)` decorator at line 136; lazy fixture resolution after skip-guard (lines 161-181) preserves single-launch semantics; module docstring cites `Phase 5.1 S1.T2 — ADR-V2-025 tier parametrization (NEW-D2)` (lines 15-19). 5 ADR-V2-025/NEW-D2 references in file. |
| `.github/workflows/phase3-contract.yml` | Matrix `include` with tier+xfail fields, hermes early-return | ✓ VERIFIED | YAML valid; 8 include rows; job-level `continue-on-error: ${{ matrix.xfail == 'true' }}` (line 57); job display name `Contract v1.1 [<tier>] — <runtime>` (line 53); hermes early-return step (lines 88-92); all 5 build/test steps gated `if: matrix.runtime != 'hermes'`; path triggers extended to ADR-V2-025*.md + Makefile. |
| `docs/design/EAASP/DEFERRED_LEDGER.md` | NEW-D2 row marked CLOSED with commit hashes | ✓ VERIFIED | NEW- namespace section added (lines 444-454); NEW-D2 row at line 450 references T1+T2+T3 commit hashes; 状态变更日志 line 377 logs P1→CLOSED transition with T2 hash; 统计汇总 NEW- counter row at line 394. 最后更新 line 6 refreshed to 2026-05-02. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| ADR-V2-025 | test_chunk_type_contract.py | `enforcement.trace[0]` | ✓ WIRED | YAML frontmatter `enforcement.trace` list contains test path; test file imports `ADR-V2-021` whitelist + cites `ADR-V2-025` in module docstring. |
| ADR-V2-025 | phase3-contract.yml | `enforcement.trace[1]` | ✓ WIRED | YAML `trace[1]` lists workflow path; CI workflow header cites `Phase 5.1 S1.T3 — ADR-V2-025 tier-based gating`; path trigger watches `docs/design/EAASP/adrs/ADR-V2-025*.md`. |
| CI matrix | tier semantics | `continue-on-error: ${{ matrix.xfail == 'true' }}` | ✓ WIRED | Primary tier xfail='false' → continue-on-error evaluates to false → blocking; Sample/Reference xfail='true' → continue-on-error true → non-blocking; Frozen xfail='skip' + step-level `if: matrix.runtime != 'hermes'` → no build/test invocation. Plan AC §T3 satisfied verbatim. |
| NEW-D2 closure | Phase 5.1 commits | DEFERRED_LEDGER row text | ✓ WIRED | Row 450 cites all 3 implementation commits; status-log row 377 cites T2 hash; commits exist in `git log` (verified `7293868`, `3ba59a1`, `e6c993b`, `c4440ef`). |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| ADR YAML parses | `python3 -c 'yaml.safe_load(open("ADR-V2-025...md").read().split("---")[1])'` | Returns dict with id/title/status=Accepted/related=[V2-017,V2-021,V2-024] | ✓ PASS |
| CI YAML parses | `python3 -c 'yaml.safe_load(open(".github/workflows/phase3-contract.yml"))'` | "CI YAML valid" | ✓ PASS |
| Test collection | `pytest cases/test_chunk_type_contract.py --collect-only -q` | "9 tests collected in 0.01s" — 7 parametrized + 2 guard | ✓ PASS |
| 4 tier names | `grep -oE '主力档\|样板档\|参考档\|冻结档' \| sort -u \| wc -l` | 4 | ✓ PASS |
| 7 runtimes in ADR | `grep -oE '\bgrid\b\|\bclaude-code\b\|...' \| sort -u` | All 7: ccb, claude-code, claw-code, goose, grid, nanobot, pydantic-ai | ✓ PASS |
| Status Accepted | `grep '^status: Accepted' ADR-V2-025*.md` | Match at line 5 | ✓ PASS |
| Tier matrix entries | `grep -cE "xfail: '" phase3-contract.yml` | 8 (7 active + hermes) | ✓ PASS |
| REQUIREMENTS marked | `grep -E "^- \[x\] \*\*(CONTRACT-00\|WATCH-05)\*\*"` | Both present with `[x]` | ✓ PASS |
| All 4 task commits exist | `git log --oneline --all \| grep -E '7293868\|3ba59a1\|e6c993b\|c4440ef'` | 4 matches | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| CONTRACT-00 | 05.1-01-PLAN | 7 runtime 分级 review + 契约执行强度策略 ADR (ADR-V2-025 候选) | ✓ SATISFIED | ADR-V2-025 Accepted with 4-tier × 7-runtime table; REQUIREMENTS.md line 30 marked `- [x]` |
| WATCH-05 | 05.1-01-PLAN | NEW-D2 — test_chunk_type_contract.py 7-runtime 参数化 | ✓ SATISFIED | Test parametrized; pytest --collect-only shows 9 items (7+2); NEW-D2 closed in DEFERRED_LEDGER; REQUIREMENTS.md line 41 marked `- [x]` |

No orphaned requirements: REQUIREMENTS.md Traceability table maps only CONTRACT-00 + WATCH-05 to phase 5.1, both addressed.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | None detected | — | No TODO/FIXME/PLACEHOLDER markers, no empty implementations, no static returns in modified files. The `try/except Exception: pass` in `test_chunk_type_contract.py:231-234` is documented (D139 owns double-Terminate semantics) — not a stub. |

### Human Verification Required

None. All success criteria verifiable programmatically via grep / yaml parse / pytest --collect-only. The CI tier-gating behavior would require an actual PR run to fully observe in GitHub Actions UI, but the workflow structure is YAML-valid, the matrix shape matches Plan T3 verbatim, and the `continue-on-error` interpolation is a documented GitHub Actions pattern — sufficient for static verification.

### Gaps Summary

No gaps. All 4 ROADMAP success criteria satisfied. Plan executed exactly as specified. The ROADMAP SC #4 phrasing "≥ 21 (3 cases × 7 runtime)" is reconciled per the Plan T2 acceptance criterion: only the live `test_chunk_type_contract` is parametrized over 7 runtimes (the 2 guard tests are proto-schema invariants with no runtime dependency, by design), giving 7 parametrized + 2 guard = 9 collected items. The Plan SUMMARY explicitly addresses this in §Deviations: "21 mention is best read as an upper bound (3 functions × 7 runtimes if all were parametrized), not a strict acceptance criterion — strict requirement is ≥7 for the live test, which is met." User-supplied verification note in this verification request explicitly endorses this interpretation.

---

*Verified: 2026-05-02*
*Verifier: Claude (gsd-verifier)*
