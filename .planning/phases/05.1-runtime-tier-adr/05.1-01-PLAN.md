---
id: 05.1-01
title: "Runtime Tier ADR + Contract Test Parametrization"
wave: 1
depends_on: []
autonomous: false
requirements_addressed:
  - CONTRACT-00
  - WATCH-05
files_modified:
  - "docs/design/EAASP/adrs/ADR-V2-025-l1-runtime-contract-tier-strategy.md"
  - "tests/contract/cases/test_chunk_type_contract.py"
  - ".github/workflows/phase3-contract.yml"
  - "docs/design/EAASP/DEFERRED_LEDGER.md"
  - "Makefile"
---

# Plan 05.1-01: Runtime Tier ADR + Contract Test Parametrization

## Objective

Draft ADR-V2-025 (L1 Runtime Contract Tier Strategy) defining 4 execution tiers × 7 runtimes, expand `test_chunk_type_contract.py` to 7-runtime parametrized (≥21 cases), update CI workflow to enforce tier behavior, and close NEW-D2 in DEFERRED_LEDGER.

## Background

Phase 5.0 completed hook envelope parity (D134 resolved). Phase 5.3 contract-v1.2.0 upgrade requires ADR-V2-025 to be **Accepted** before it can determine which runtimes must PASS vs may degrade. NEW-D2 (WATCH-05) tracks the test parametrization gap: `test_chunk_type_contract.py` currently has 3 tests (not 7-runtime × 58 cases as the conftest docs claimed).

## Tasks

### T1 — Draft and Accept ADR-V2-025

<read_first>
- docs/design/EAASP/adrs/ADR-V2-017-l1-runtime-ecosystem-strategy.md
- docs/design/EAASP/adrs/ADR-V2-021-chunk-type-contract-freeze.md
- docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md
</read_first>

**Action:**
1. Create `docs/design/EAASP/adrs/ADR-V2-025-l1-runtime-contract-tier-strategy.md` following ADR frontmatter pattern from ADR-V2-017:
   - `id: ADR-V2-025`
   - `title: "L1 Runtime 契约执行强度差异化策略（四档）"`
   - `type: strategy`
   - `status: Proposed` (→ Accepted after F1-F4 lint)
   - `phase: "Phase 5.1 — Engine Hardening"`
   - `author: "Jiangwen Su"`

2. Decision section contains a table with **4 tiers × 7 active runtimes** (hermes shown as frozen note):

   | Tier (执行强度) | Behavior | grid | claude-code | nanobot | pydantic-ai | goose | claw-code | ccb |
   |---|---|---|---|---|---|---|---|---|
   | **主力档 (Primary)** | MUST PASS; xfail = blocker | ✅ | ✅ | — | — | — | — | — |
   | **样板档 (Sample)** | PASS-or-xfail; regressions must be investigated | — | — | ✅ | — | ✅ | — | — |
   | **参考档 (Reference)** | v1.1 baseline; no new failures required | — | — | — | ✅ | — | ✅ | ✅ |
   | **冻结档 (Frozen)** | skip; hermes frozen per ADR-V2-017 | ⏸️ | — | — | — | — | — | — |

   **Justification rationale** (each tier — 2-3 sentences per runtime):
   - Primary: grid (main substitutable runtime), claude-code (CLI-first runtime, Phase 4 shipped)
   - Sample: nanobot (Phase 3 certified), goose (Phase 2.5 W1 certified)
   - Reference: pydantic-ai (early adopter, no formal cert), claw-code (emerging), ccb (TypeScript reference impl)
   - Frozen: hermes (per ADR-V2-017 §Frozen)

3. §References block cites:
   - ADR-V2-017 (产品三轨 — grid / claude-code / goose)
   - ADR-V2-021 (chunk_type contract — whitelist, UNSPECIFIED forbidden, DONE required)
   - ADR-V2-024 (Phase 4 product scope — dual-axis framework)
   - NEW-D2 (Phase 4a project review — test parametrization gap)

4. §4 (Verification) references:
   - `tests/contract/cases/test_chunk_type_contract.py` (parametrized 7-runtime)
   - `.github/workflows/phase3-contract.yml` (tier-gated matrix)

5. F1-F4 lint gate (from ADR-V2-017 pattern):
   - F1: `grep -E '^\-\-\-' docs/design/EAASP/adrs/ADR-V2-025*.md | wc -l` → must be 2 (valid YAML frontmatter)
   - F2: `grep 'status: Accepted' docs/design/EAASP/adrs/ADR-V2-025*.md` → exit 0 after status update
   - F3: Table has all 7 runtimes + hermes frozen row
   - F4: §References cites V2-017, V2-021, V2-024, NEW-D2

**Acceptance Criteria:**
- `test -f docs/design/EAASP/adrs/ADR-V2-025-l1-runtime-contract-tier-strategy.md`
- `grep 'status: Accepted' docs/design/EAASP/adrs/ADR-V2-025*.md` exits 0
- `grep -E '主力档|样板档|参考档|冻结档' docs/design/EAASP/adrs/ADR-V2-025*.md` → 4 tier names found
- `grep 'grid' docs/design/EAASP/adrs/ADR-V2-025*.md` → grid appears in tier table
- F1: frontmatter valid YAML (`python3 -c 'import yaml; yaml.safe_load(open("docs/design/EAASP/adrs/ADR-V2-025-l1-runtime-contract-tier-strategy.md"))'`)

---

### T2 — Parametrize test_chunk_type_contract.py for 7 Runtimes

<read_first>
- tests/contract/cases/test_chunk_type_contract.py
- tests/contract/conftest.py
- .github/workflows/phase3-contract.yml
</read_first>

**Action:**
1. Read `conftest.py` to confirm `runtime_name` fixture and the existing 7-runtime parametrize pattern used by other contract test files (e.g., `test_event_type.py` or `test_proto_shape.py` if they exist).

2. Add `@pytest.mark.parametrize("runtime_name", ["grid", "claude-code", "nanobot", "pydantic-ai", "goose", "claw-code", "ccb"])` to `test_chunk_type_contract.py`.

   **Note:** `test_whitelist_matches_adr()` and `test_unspecified_is_zero()` are guard tests that don't need runtime parametrization — they run against the proto library directly. Only `test_chunk_type_contract()` needs the runtime parametrize because it launches a real runtime subprocess.

3. Verify the parametrized test is recognized:
   - `python3 -m pytest tests/contract/cases/test_chunk_type_contract.py --collect-only -q` should show ≥7 items for `test_chunk_type_contract`

4. Count total test cases: 3 test functions × 7 runtimes = 21 parametrized cases total (from ROADMAP SC: "≥21 (3 cases × 7 runtime)")

5. Add module-level docstring note citing ADR-V2-025:
   ```
   Phase 5.1 S1.T2 — ADR-V2-025 tier parametrization (NEW-D2).
   主力档 (grid, claude-code): MUST PASS.
  样板档 (nanobot, goose): PASS-or-xfail.
   参考档 (pydantic-ai, claw-code, ccb): v1.1 baseline.
   hermes: frozen (ADR-V2-017).
   ```

**Acceptance Criteria:**
- `python3 -m pytest tests/contract/cases/test_chunk_type_contract.py --collect-only -q` shows ≥7 parametrized items for `test_chunk_type_contract`
- `grep 'ADR-V2-025' tests/contract/cases/test_chunk_type_contract.py` exits 0
- `grep '@pytest.mark.parametrize' tests/contract/cases/test_chunk_type_contract.py` exits 0
- `grep 'NEW-D2' tests/contract/cases/test_chunk_type_contract.py` exits 0

---

### T3 — Update CI Workflow for Tier Behavior

<read_first>
- .github/workflows/phase3-contract.yml
- docs/design/EAASP/adrs/ADR-V2-025-l1-runtime-contract-tier-strategy.md
</read_first>

**Action:**
1. In `.github/workflows/phase3-contract.yml`, add `include` rules to the matrix for hermes (always skip) and add tier markers as job names.

   Replace the matrix section with:

   ```yaml
   strategy:
     fail-fast: false
     matrix:
       include:
         - runtime: grid
           tier: primary
           xfail: false
         - runtime: claude-code
           tier: primary
           xfail: false
         - runtime: nanobot
           tier: sample
           xfail: true
         - runtime: goose
           tier: sample
           xfail: true
         - runtime: pydantic-ai
           tier: reference
           xfail: true
         - runtime: claw-code
           tier: reference
           xfail: true
         - runtime: ccb
           tier: reference
           xfail: true
         - runtime: hermes
           tier: frozen
           xfail: skip
   ```

2. Add a step after checkout that skips hermes:
   ```yaml
   - name: Skip hermes (frozen per ADR-V2-017)
     if: matrix.runtime == 'hermes'
     run: echo "hermes runtime frozen — skipping"
   ```

3. For **primary tier** (`xfail: false`): no changes to `continue-on-error` — failures block the PR.
   For **sample/reference tier** (`xfail: true`): add `continue-on-error: true` to the job.

4. Update job name to include tier: `Contract v1.1 [${{ matrix.tier }}] — ${{ matrix.runtime }}`

5. Update path triggers to also watch `docs/design/EAASP/adrs/ADR-V2-025*.md` and `Makefile`.

**Acceptance Criteria:**
- `grep 'xfail: false' .github/workflows/phase3-contract.yml` → grid + claude-code entries
- `grep 'xfail: true' .github/workflows/phase3-contract.yml` → nanobot + goose + pydantic-ai + claw-code + ccb entries
- `grep 'hermes' .github/workflows/phase3-contract.yml` → frozen tier entry
- `grep 'ADR-V2-025' .github/workflows/phase3-contract.yml` → path trigger updated
- YAML validates: `python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/phase3-contract.yml"))'`

---

### T4 — Close NEW-D2 in DEFERRED_LEDGER

<read_first>
- docs/design/EAASP/DEFERRED_LEDGER.md
- .planning/REQUIREMENTS.md
</read_first>

**Action:**
1. In `docs/design/EAASP/DEFERRED_LEDGER.md`, add a NEW-D2 row under the D145+ section (or create a new Phase 5.1 section):

   ```
   | NEW-D2 | `test_chunk_type_contract.py` only 3 tests, not 7-runtime × 58 cases as conftest claimed | Phase 5.1 T2 | ✅ CLOSED | Phase 5.1 T2 @ `<commit-hash>` — parametrized 7-runtime (21 cases); ADR-V2-025 tier strategy defines 4 tiers |
   ```

2. In the **统计汇总** section, add NEW-D2 to the ✅ closed list.

3. In the **状态变更日志** table, append:
   ```
   | <date> | NEW-D2 | 🟠 P1 → ✅ CLOSED | Phase 5.1 T2 — parametrized test_chunk_type_contract.py (21 cases) + ADR-V2-025 |
   ```

4. Update the **最后更新** line at the top of DEFERRED_LEDGER.md to today's date.

**Acceptance Criteria:**
- `grep 'NEW-D2' docs/design/EAASP/DEFERRED_LEDGER.md` exits 0
- `grep '✅ CLOSED' docs/design/EAASP/DEFERRED_LEDGER.md | grep NEW-D2` exits 0 (if existing)
- `grep '21' docs/design/EAASP/DEFERRED_LEDGER.md` exits 0 (test case count)
- `grep 'ADR-V2-025' docs/design/EAASP/DEFERRED_LEDGER.md` exits 0

---

## Verification

| Check | Command |
|-------|---------|
| ADR-V2-025 Accepted | `grep 'status: Accepted' docs/design/EAASP/adrs/ADR-V2-025*.md` |
| ADR frontmatter valid | `python3 -c 'import yaml; yaml.safe_load(open("docs/design/EAASP/adrs/ADR-V2-025-l1-runtime-contract-tier-strategy.md"))'` |
| ADR §References | `grep 'ADR-V2-017\|ADR-V2-021\|ADR-V2-024\|NEW-D2' docs/design/EAASP/adrs/ADR-V2-025*.md` |
| 4 tier names | `grep -c '主力档\|样板档\|参考档\|冻结档' docs/design/EAASP/adrs/ADR-V2-025*.md` → 4 |
| Test parametrized | `python3 -m pytest tests/contract/cases/test_chunk_type_contract.py --collect-only -q` |
| CI tier matrix | `grep -c 'xfail' .github/workflows/phase3-contract.yml` → ≥7 |
| CI hermes frozen | `grep 'hermes' .github/workflows/phase3-contract.yml` |
| NEW-D2 closed | `grep 'NEW-D2.*✅ CLOSED' docs/design/EAASP/DEFERRED_LEDGER.md` |

## must_haves (goal-backward verification)

1. ADR-V2-025 status is **Accepted** — this unlocks Phase 5.3 contract-v1.2.0 tier-based gating
2. `test_chunk_type_contract.py` runs **≥21 parametrized cases** (3 tests × 7 runtimes)
3. CI workflow matrix enforces **primary tier = blocking**, sample/reference = `continue-on-error`, hermes = skip
4. NEW-D2 is **CLOSED** with commit hash in DEFERRED_LEDGER
5. Phase 5.3 can use ADR-V2-025 to determine which runtime failures block v1.2.0 release
