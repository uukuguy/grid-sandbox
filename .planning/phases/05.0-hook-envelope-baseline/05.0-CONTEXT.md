# Phase 5.0: Hook Envelope Baseline - Context

**Gathered:** 2026-05-19
**Status:** Ready for planning

<domain>
## Phase Boundary

Hook envelope baseline health patch — fix drift between ADR-V2-006 contract (flat top-level envelope fields) and shipped hook scripts that carry dead dual-path `.output.*` checks. Verify Stop envelope field parity between Rust and Python runtimes.

Scope:
- Fix shipped hook scripts (`.output.*` dual-path dead code)
- Verify/expand Stop envelope contract test coverage
- Audit D134 "已落盘" completeness

Out of scope: new capabilities, engine-level changes to envelope schema.

</domain>

<decisions>
## Implementation Decisions

All three gray areas were **deferred** to implementation planning. Decisions to be made during `/gsd-plan-phase 5.0`:

### GA1: `.output.*` dual-path dead code
- **Status:** Deferred
- **Known:** ADR-V2-006 §2.3 defines flat top-level fields; engine never emits `.output.*`; shipped hooks carry dead dual-path checks in both `check_output_anchor.sh` and `check_final_output.sh`
- **Decision needed:** Delete dead checks (Option 1), keep as hedge (Option 2), or version-flag hooks

### GA2: Python runtime parity — Stop envelope fields
- **Status:** Deferred
- **Known:** `hook_envelope_parity_test.rs` covers Stop envelope base shape but does NOT test `reason`, `stop_type`, `full_completion` fields per ADR-V2-006 §2.3
- **Decision needed:** Add coverage or skip

### GA3: D134 "已落盘" completeness audit
- **Status:** Deferred
- **Known:** Hook scripts shipped; `test_hook_envelope.py` exists but Stop coverage is minimal; `hook_envelope_parity_test.rs` covers Rust parity but not Python Stop-specific fields
- **Decision needed:** Audit test coverage or close D134 as-is

### Claude's Discretion
- Exact deletion approach for dead `.output.*` checks (full removal vs comment-out vs versioned)
- Test naming and placement conventions for new Stop envelope parity tests
- Whether to file a new D-number or reuse existing for any coverage gaps found

</decisions>

<specifics>
## Specific Ideas

No specific references from discussion — all decisions deferred.

</specifics>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Hook Envelope Contract
- `docs/design/EAASP/adrs/ADR-V2-006-hook-envelope-contract.md` — Hook envelope schema (PreToolUse / PostToolUse / Stop), flat top-level field requirements, §2.1–§2.3 canonical examples
- `docs/design/EAASP/adrs/ADR-V2-006-hook-envelope-contract.md` §2.3 — Stop envelope fields: `event`, `session_id`, `skill_id`, `draft_memory_id`, `evidence_anchor_id`, `reason`, `stop_type`, `full_completion`

### Shipped Hook Scripts
- `examples/skills/threshold-calibration/hooks/check_output_anchor.sh` — `.output.*` dual-path dead code location
- `examples/skills/threshold-calibration/hooks/check_final_output.sh` — `.output.*` dual-path dead code location

### Contract Tests
- `crates/grid-engine/tests/hook_envelope_parity_test.rs` — Rust/grid-engine parity tests; covers Stop base shape (event/session_id/skill_id/draft_memory_id/evidence_anchor_id) but NOT reason/stop_type/full_completion
- `tests/contract/contract_v1/test_hook_envelope.py` — Python runtime contract test suite; Stop coverage limited to required fields
- `crates/grid-engine/src/hooks/context.rs` — Rust `HookContext` serialization; confirms flat top-level fields only

### Watchlist Items
- `.planning/WATCHLIST.md` — D134 (已落盘), D120 (parity gaps), D93 (hook envelope v1.2)
- `.planning/research/PHASE-5-0-GRAY-AREAS.md` — Prior gray areas research with full analysis

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `hook_envelope_parity_test.rs` — existing parity test framework; extend for Stop-specific fields
- `tests/contract/harness/assertions.py` — shared assertion helpers (`assert_hook_envelope_required_fields`, `assert_grid_env_vars_present`)

### Established Patterns
- ADR-V2-006 flat top-level schema: `draft_memory_id` and `evidence_anchor_id` are always top-level, never nested under `.output`
- Contract test naming: `test_{scope}_envelope_has_all_required_fields`
- Hook script comment convention: explains legacy compat paths before removal

### Integration Points
- `crates/grid-engine/src/hooks/context.rs` — HookContext serialization site (source of truth for flat fields)
- `crates/grid-engine/src/agent/harness.rs` — Stop envelope emission site (threading `last_draft_memory_id`, `last_evidence_anchor_id`)
- Python runtime `service.py` — `_dispatch_scoped_stop` equivalent (needs parity verification)

</code_context>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>

---

*Phase: 05.0-hook-envelope-baseline*
*Context gathered: 2026-05-19*
