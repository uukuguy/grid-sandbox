---
phase: 05.0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/skills/threshold-calibration/hooks/check_output_anchor.sh
  - examples/skills/skill-extraction/hooks/check_final_output.sh
  - crates/grid-engine/tests/hook_envelope_parity_test.rs
  - tests/contract/contract_v1/test_hook_envelope.py
autonomous: true
requirements:
  - WATCH-00
  - WATCH-02
must_haves:
  truths:
    - Shipped skill hooks use flat key paths per ADR-V2-006
    - D120 parity test suite passes 100%
    - D134 nested key path conflict is resolved
  artifacts:
    - path: examples/skills/threshold-calibration/hooks/check_output_anchor.sh
      provides: Fixed dead code removed, flat ADR-V2-006 structure documented
    - path: examples/skills/skill-extraction/hooks/check_final_output.sh
      provides: Fixed dead code removed, flat ADR-V2-006 structure documented
    - path: crates/grid-engine/tests/hook_envelope_parity_test.rs
      provides: Stop scope parity test coverage
    - path: tests/contract/contract_v1/test_hook_envelope.py
      provides: Python runtime envelope contract assertions
  key_links:
    - from: check_output_anchor.sh
      to: ADR-V2-006
      via: jq flat key access
      pattern: .evidence_anchor_id
    - from: check_final_output.sh
      to: ADR-V2-006
      via: jq flat key access
      pattern: .draft_memory_id and .evidence_anchor_id
---
<objective>
Fix hook scripts to comply with ADR-V2-006 flat key path structure per WATCH-00. Resolve D120 parity gap by adding stop scope coverage. Clean up D134 dead code in example hooks.
</objective>

<context>
@.planning/phases/05.0-hook-envelope-baseline/05.0-CONTEXT.md
@.planning/phases/05.0-hook-envelope-baseline/05.0-RESEARCH.md
@.planning/phases/05.0-hook-envelope-baseline/05.0-DISCUSSION-LOG.md
@crates/grid-engine/tests/hook_envelope_parity_test.rs
@examples/skills/threshold-calibration/hooks/check_output_anchor.sh
@examples/skills/skill-extraction/hooks/check_final_output.sh
</context>

<tasks>

<task type="auto">
  <name>Fix check_output_anchor.sh: remove dead code, verify flat key paths</name>
  <files>examples/skills/threshold-calibration/hooks/check_output_anchor.sh</files>
  <action>
Read the file, then:
1. Remove any dead code (D134 GA1): lines that are commented out, unreachable code, or code that references nested structures that no longer exist. Specifically check for nested path patterns like .memory.evidence_anchor_id or .output.memory that should now be flat .evidence_anchor_id per ADR-V2-006.
2. Verify jq accesses use flat key paths: .evidence_anchor_id (not .memory.evidence_anchor_id)
3. Keep all functional logic intact. Only remove dead/obsolete code.
4. Do NOT change working functionality — only remove confirmed dead code.
</action>
  <verify>grep -c '\.memory\.' examples/skills/threshold-calibration/hooks/check_output_anchor.sh; echo "Should return 0 (no nested memory paths)"</verify>
  <done>check_output_anchor.sh has no nested memory key paths, dead code removed, flat ADR-V2-006 compliant jq accesses</done>
</task>

<task type="auto">
  <name>Fix check_final_output.sh: remove dead code, verify flat key paths</name>
  <files>examples/skills/skill-extraction/hooks/check_final_output.sh</files>
  <action>
Read the file, then:
1. Remove any dead code (D134 GA1): lines that are commented out, unreachable code, or code that references nested structures that no longer exist. Specifically check for nested path patterns like .memory.draft_memory_id or .output.memory that should now be flat .draft_memory_id and .evidence_anchor_id per ADR-V2-006.
2. Verify jq accesses use flat key paths: .draft_memory_id, .evidence_anchor_id (not nested)
3. Keep all functional logic intact. Only remove dead/obsolete code.
4. Do NOT change working functionality — only remove confirmed dead code.
</action>
  <verify>grep -c '\.memory\.' examples/skills/skill-extraction/hooks/check_final_output.sh; echo "Should return 0 (no nested memory paths)"</verify>
  <done>check_final_output.sh has no nested memory key paths, dead code removed, flat ADR-V2-006 compliant jq accesses</done>
</task>

<task type="auto">
  <name>Add stop scope parity test to hook_envelope_parity_test.rs</name>
  <files>crates/grid-engine/tests/hook_envelope_parity_test.rs</files>
  <action>
Read the existing test file, then add test_stop_envelope_parity function that:
1. Creates a Stop Envelope with known field values (stop_reason, full_completion, tokens_used, draft_memory_id, evidence_anchor_id)
2. Serializes it to JSON using serde_json
3. Verifies the JSON contains flat key paths (not nested under "memory")
4. Uses the existing StdOutEnvelope parity test as a reference pattern

Key requirements per D120 WATCH-02:
- Stop envelopes must have flat key structure
- .draft_memory_id and .evidence_anchor_id must be top-level (not .memory.*)
- Test must FAIL if flat structure is violated
</action>
  <verify>cargo test -p grid-engine hook_envelope_parity_test -- --nocapture 2>&1 | tail -20</verify>
  <done>test_stop_envelope_parity function exists, compiles, and passes. All existing tests continue to pass.</done>
</task>

</tasks>

<verification>
1. Grep gate: Both hook scripts have zero nested .memory. paths
2. Rust tests: cargo test -p grid-engine hook_envelope_parity_test passes 100%
3. Dead code audit: No commented-out code blocks referencing nested memory paths
4. D134 closure: GA1 = dead code removed, GA2 = doc error fixed, GA3 = out of scope
</verification>

<success_criteria>
- Both hook scripts use flat ADR-V2-006 key paths only
- cargo test passes: all hook_envelope_parity_test.rs tests pass
- D134 documented as CLOSED in phase SUMMARY via GA1 resolution
</success_criteria>

<output>
After completion, create `.planning/phases/05.0-hook-envelope-baseline/05.0-01-SUMMARY.md`
</output>
